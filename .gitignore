# Build output
build/

# Machine-specific local overrides (Qt paths etc.)
CMakeUserPresets.json

# Python client: virtualenv and generated test artifacts
client/.venv/
client/shots/
client/scratch/
__pycache__/
*.pyc

# Third-party reference project (qt-mcp, Python/PySide6) — kept locally as the
# design blueprint, not part of this codebase
qt-mcp/

# Local Qt-ADS experiment workspace (see examples/qt-ads/), helper scripts and
# runtime-generated settings — not part of this codebase
tmp/
build_demo.bat
client/Settings.ini
.cache/
examples/qt-ads/shots/

# Private-host embedding workspace (guide/patch/test scripts for a proprietary
# Qt app) — deliberately not committed for now
examples/SRMStudioX/
