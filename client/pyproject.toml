[project]
name = "qtmcp-client"
version = "0.1.0"
description = "MCP client scripts for verifying QtMcpEmbedded against demo_app"
requires-python = ">=3.10"
dependencies = ["mcp>=1.10"]

[tool.uv]
package = false
